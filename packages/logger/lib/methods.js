/**
 * Names of all log methods (correspond to the log levels)
 * @typedef {"debug" | "info" | "warn" | "error"} Method
 */

/**
 * Names of all log methods (correspond to the log levels)
 * @type {Method[]}
 */
export const methods = ["debug", "info", "warn", "error"];
