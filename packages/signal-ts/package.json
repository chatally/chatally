{
  "name": "signal-ts",
  "version": "1.0.0",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "rimraf dist && esbuild src/*.ts --bundle --platform=node --packages=external --outdir=dist",
    "build:ci": "tsc && npm run build",
    "protos": "node --import tsx scripts/compile-protos.ts",
    "debug": "nodemon --exec \"node --import tsx\" debug.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/lodash": "^4.17.7",
    "@types/node": "^20.10.2",
    "concurrently": "^8.2.2",
    "esbuild": "0.23.0",
    "nodemon": "^3.0.2",
    "rimraf": "^6.0.1",
    "tsx": "^4.6.2",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@chatally/logger": "^0.0.8",
    "@signalapp/better-sqlite3": "^8.7.1",
    "chalk": "^5.3.0",
    "fastify": "^4.24.3",
    "lodash": "^4.17.21",
    "long": "^5.2.3",
    "nano": "^10.1.3",
    "p-queue": "^8.0.1",
    "p-timeout": "^6.1.2",
    "protobufjs": "^7.3.2",
    "proxy-agent": "^6.4.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20.6"
  }
}
